diff --git a/src/main_panel.cpp b/src/main_panel.cpp
index d414648..c1de85d 100644
--- a/src/main_panel.cpp
+++ b/src/main_panel.cpp
@@ -53,7 +53,15 @@ MainPanel::MainPanel(KWebSocketClient &websocket,
   , action_btn(main_cont, &fan, "Fans", &MainPanel::_handle_fanpanel_cb, this)
   , led_btn(main_cont, &light_img, "LED", &MainPanel::_handle_ledpanel_cb, this)
   , print_btn(main_cont, &print, "Print", &MainPanel::_handle_print_cb, this)
+  , filament_change_btn(main_cont, &filament_img, "Filament Change", &MainPanel::_handle_filament_change_cb, this)
 {
+    prompt_panel.set_open_spoolman_callback([this]() { spoolman_panel.foreground(); });
+    spoolman_panel.set_back_callback([this]() {
+      if (prompt_panel.is_active()) {
+        prompt_panel.foreground();
+      }
+    });
+
     lv_style_init(&style);
     lv_style_set_img_recolor_opa(&style, LV_OPA_30);
     lv_style_set_img_recolor(&style, lv_color_black());
@@ -191,6 +199,12 @@ void MainPanel::handle_print_cb(lv_event_t *event) {
   }
 }
 
+void MainPanel::handle_filament_change_cb(lv_event_t *event) {
+  if (lv_event_get_code(event) == LV_EVENT_CLICKED) {
+    ws.gcode_script("FILAMENT_CHANGE");
+  }
+}
+
 void MainPanel::create_main(lv_obj_t * parent)
 {
     lv_obj_set_flex_flow(parent, LV_FLEX_FLOW_ROW_WRAP);
@@ -210,6 +224,7 @@ void MainPanel::create_main(lv_obj_t * parent)
     lv_obj_set_grid_cell(action_btn.get_container(), LV_GRID_ALIGN_CENTER, 2, 1, LV_GRID_ALIGN_CENTER, 1, 1);
     lv_obj_set_grid_cell(led_btn.get_container(), LV_GRID_ALIGN_CENTER, 3, 1, LV_GRID_ALIGN_CENTER, 1, 1);
     lv_obj_set_grid_cell(print_btn.get_container(), LV_GRID_ALIGN_CENTER, 2, 2, LV_GRID_ALIGN_CENTER, 2, 1);
+    lv_obj_set_grid_cell(filament_change_btn.get_container(), LV_GRID_ALIGN_CENTER, 0, 2, LV_GRID_ALIGN_CENTER, 2, 1);
 
     lv_obj_clear_flag(temp_cont, LV_OBJ_FLAG_SCROLLABLE);
     lv_obj_set_size(temp_cont, LV_PCT(50), LV_PCT(50));
diff --git a/src/main_panel.h b/src/main_panel.h
index 39f3191..dc7a761 100644
--- a/src/main_panel.h
+++ b/src/main_panel.h
@@ -46,6 +46,7 @@ class MainPanel : public NotifyConsumer {
   void handle_fanpanel_cb(lv_event_t *event);
   void handle_ledpanel_cb(lv_event_t *event);
   void handle_print_cb(lv_event_t *event);
+  void handle_filament_change_cb(lv_event_t *event);
 
   lv_obj_t *create_button(lv_obj_t *parent,
 			  const void *btn_img,
@@ -82,6 +83,11 @@ class MainPanel : public NotifyConsumer {
     panel->handle_print_cb(event);
   };
 
+  static void _handle_filament_change_cb(lv_event_t *event) {
+    MainPanel *panel = (MainPanel*)event->user_data;
+    panel->handle_filament_change_cb(event);
+  };
+
  private:
   void create_main(lv_obj_t *parent);
   KWebSocketClient &ws;
@@ -118,5 +124,6 @@ class MainPanel : public NotifyConsumer {
   ButtonContainer action_btn;
   ButtonContainer led_btn;
   ButtonContainer print_btn;
+  ButtonContainer filament_change_btn;
 };
 #endif // __MAIN_PANEL_H__
diff --git a/src/prompt_panel.cpp b/src/prompt_panel.cpp
index 1eb68b5..d1ece22 100644
--- a/src/prompt_panel.cpp
+++ b/src/prompt_panel.cpp
@@ -3,6 +3,10 @@
 #include "utils.h"
 #include "spdlog/spdlog.h"
 
+#include <algorithm>
+#include <exception>
+#include <utility>
+
 // uncomment for helper boxes
 // #define DEBUG_LINES
 
@@ -20,6 +24,8 @@ PromptPanel::PromptPanel(KWebSocketClient &websocket_client, std::mutex &lock, l
     , flex(lv_obj_create(prompt_cont))
     , header(lv_label_create(prompt_cont))
     , footer_cont(lv_obj_create(prompt_cont))
+    , progress_bar(NULL)
+    , prompt_active(false)
 //  , back_btn(promptpanel_cont, &back, "Back", &PromptPanel::_handle_callback, this)
 {
     lv_obj_set_style_pad_all(prompt_cont, 0, 0);
@@ -149,6 +155,14 @@ void PromptPanel::background() {
   lv_obj_move_background(prompt_cont);
 }
 
+void PromptPanel::set_open_spoolman_callback(std::function<void()> callback) {
+    open_spoolman_callback = std::move(callback);
+}
+
+bool PromptPanel::is_active() const {
+    return prompt_active;
+}
+
 void PromptPanel::handle_callback(lv_event_t *event) {
     lv_obj_t *btn = lv_event_get_current_target(event);
 
@@ -220,6 +234,7 @@ void PromptPanel::handle_macro_response(json &j) {
             if (command.find("prompt_begin") == 0) {
                 std::string prompt_header = command.substr(13);
                 spdlog::debug("PROMPT_BEGIN: {}", prompt_header);
+                prompt_active = true;
 
                 // remove buttons
                 lv_obj_clean(footer_cont);
@@ -275,6 +290,28 @@ void PromptPanel::handle_macro_response(json &j) {
                 // does nothing since start creates a new one
                 spdlog::debug("Button group ended");
                 button_group_cont = NULL;
+            } else if (command.find("prompt_spoolman") == 0) {
+                if (open_spoolman_callback) {
+                    open_spoolman_callback();
+                }
+            } else if (command.find("prompt_progress") == 0) {
+                std::string value = command.substr(strlen("prompt_progress"));
+                int progress;
+                try {
+                    progress = std::stoi(value);
+                } catch (const std::exception &e) {
+                    spdlog::warn("Ignoring invalid prompt_progress value '{}': {}", value, e.what());
+                    return;
+                }
+                progress = std::max(0, std::min(100, progress));
+                if (progress_bar == NULL) {
+                    progress_bar = lv_bar_create(flex);
+                    lv_obj_set_width(progress_bar, lv_pct(90));
+                    lv_obj_set_height(progress_bar, 24);
+                    lv_obj_center(progress_bar);
+                    lv_bar_set_range(progress_bar, 0, 100);
+                }
+                lv_bar_set_value(progress_bar, progress, LV_ANIM_OFF);
             } else if (command.find("prompt_footer_button") == 0 || command.find("prompt_button") == 0) {
                 int index_label = command.find("button", 0) + strlen("button");
                 int index_first = command.find("|", index_label);
@@ -349,6 +386,8 @@ void PromptPanel::handle_macro_response(json &j) {
                 foreground();
             } else if (command.find("prompt_end") == 0) {
                 spdlog::debug("PROMPT_END");
+                progress_bar = NULL;
+                prompt_active = false;
                 background();
 
                 // remove buttons
diff --git a/src/prompt_panel.h b/src/prompt_panel.h
index 9089972..2fda82c 100644
--- a/src/prompt_panel.h
+++ b/src/prompt_panel.h
@@ -9,6 +9,7 @@
 #include <map>
 #include <memory>
 #include <mutex>
+#include <functional>
 
 struct SharedButton {
     SharedButton(lv_obj_t *bbtn) : btn(bbtn) {};
@@ -33,6 +34,8 @@ class PromptPanel : public NotifyConsumer {
 
         void foreground();
         void background();
+        void set_open_spoolman_callback(std::function<void()> callback);
+        bool is_active() const;
 
     private:
 
@@ -45,6 +48,9 @@ class PromptPanel : public NotifyConsumer {
         lv_obj_t *header;
         lv_obj_t *button_group_cont;
         lv_obj_t *footer_cont;
+        lv_obj_t *progress_bar;
+        std::function<void()> open_spoolman_callback;
+        bool prompt_active;
 
 };
 
diff --git a/src/spoolman_panel.cpp b/src/spoolman_panel.cpp
index 22abcee..434dbea 100644
--- a/src/spoolman_panel.cpp
+++ b/src/spoolman_panel.cpp
@@ -2,6 +2,8 @@
 #include "utils.h"
 #include "spdlog/spdlog.h"
 
+#include <utility>
+
 LV_IMG_DECLARE(back);
 LV_IMG_DECLARE(refresh_img);
 
@@ -84,6 +86,10 @@ SpoolmanPanel::~SpoolmanPanel() {
   }
 }
 
+void SpoolmanPanel::set_back_callback(std::function<void()> callback) {
+  back_callback = std::move(callback);
+}
+
 void SpoolmanPanel::init() {
   json param = {
     { "request_method", "GET" },
@@ -233,6 +239,9 @@ void SpoolmanPanel::handle_callback(lv_event_t *event) {
   if (btn == back_btn.get_container()) {
     spdlog::trace("spoolman back button pressed");
     lv_obj_move_background(cont);
+    if (back_callback) {
+      back_callback();
+    }
   } else if (btn == reload_btn.get_container()) {
     spdlog::trace("spoolman reload button pressed");
     init();
diff --git a/src/spoolman_panel.h b/src/spoolman_panel.h
index 8064f0c..e94d05a 100644
--- a/src/spoolman_panel.h
+++ b/src/spoolman_panel.h
@@ -8,6 +8,7 @@
 
 #include <map>
 #include <mutex>
+#include <functional>
 
 class SpoolmanPanel {
  public:
@@ -21,6 +22,7 @@ class SpoolmanPanel {
   
   void handle_callback(lv_event_t *event);
   void handle_spoolman_action(lv_event_t *event);
+  void set_back_callback(std::function<void()> callback);
 
   static void _handle_callback(lv_event_t *event) {
     SpoolmanPanel *panel = (SpoolmanPanel*)event->user_data;
@@ -45,6 +47,7 @@ class SpoolmanPanel {
   int32_t active_id;
   std::map<uint32_t, json> spools;
   uint32_t sorted_by;
+  std::function<void()> back_callback;
 };
 
 #endif // __SPOOLMAN_PANEL_H__
